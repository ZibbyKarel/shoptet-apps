{
  "name": "api-e2e",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["api"],
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "dependsOn": ["api:build", "api:serve"],
      "// command": "No `--passWithNoTests`: it made this target green whenever the spec files stopped matching, which is the one failure mode a suite that only ever asserts refusals cannot be trusted to notice (final review I-1). If jest finds nothing here, that is the result worth having.",
      "options": {
        "cwd": "apps/garage/api-e2e",
        "command": "jest --runInBand"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p apps/garage/api-e2e/tsconfig.spec.json"
      }
    }
  },
  "tags": ["type:app", "scope:api", "app:garage"]
}
