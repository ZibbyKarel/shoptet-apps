# Generated by `next build` on every run; must not be hand-edited
/apps/garage/web/next-env.d.ts

# Build output and caches
/dist
/coverage
/.nx/cache
/.nx/workspace-data
/apps/garage/web/.next
**/*.tsbuildinfo

# Not workspace source: hand-written docs, exported design assets, tool config
/doc
/plan.md
/.claude
# Vendored agent skill definitions (160 tracked files, third-party markdown).
# Same category as /.claude above: tool config, not workspace source. They
# arrive formatted by whoever wrote them, and `nx format:check --all` — which
# CI runs — failed on ~150 of them, so CI was red for a reason no source change
# could fix. Reformatting them instead would rewrite vendored text and make the
# next upstream sync a conflict.
/.agents
**/package-lock.json

# Generated 1:1 from TS design tokens (libs/shared/design-system/src/tokens/lib/*.ts) to
# mirror doc/design/ds/colors_and_type.css byte-for-byte (uppercase hex, no rgba()
# spacing, double quotes) — Prettier would rewrite those stylistic choices even
# though the source of truth uses them. Regenerate via
# `nx run design-system:generate-css`; never hand-edit.
/libs/shared/design-system/assets/tokens.css

# Prisma Client, generated 1:1 from libs/garage/database/prisma/schema.prisma and
# committed so that lint/typecheck/test/build need no database and no generate
# step (same reasoning as tokens.css above). Prisma owns this formatting.
# Regenerate via `npx prisma generate`; never hand-edit.
/libs/garage/database/src/generated
