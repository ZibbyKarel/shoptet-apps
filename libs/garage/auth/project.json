{
  "name": "auth",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/garage/auth/src",
  "projectType": "library",
  "tags": ["type:util", "scope:web", "app:garage"],
  "// targets": "to see all targets run: nx show project auth --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p libs/garage/auth/tsconfig.lib.json",
          "tsc --noEmit -p libs/garage/auth/tsconfig.spec.json"
        ],
        "parallel": false
      }
    }
  }
}
