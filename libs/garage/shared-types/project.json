{
  "name": "shared-types",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/garage/shared-types/src",
  "projectType": "library",
  "tags": ["type:util", "scope:shared", "layer:foundation", "app:garage"],
  "// targets": "to see all targets run: nx show project shared-types --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --noEmit -p libs/garage/shared-types/tsconfig.lib.json",
          "tsc --noEmit -p libs/garage/shared-types/tsconfig.spec.json"
        ],
        "parallel": false
      }
    }
  }
}
