{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "analytics": false,
  "defaultBase": "main",
  "namedInputs": {
    "default": ["{projectRoot}/**/*", "sharedGlobals"],
    "production": [
      "default",
      "!{projectRoot}/.eslintrc.json",
      "!{projectRoot}/eslint.config.mjs",
      "!{projectRoot}/**/?(*.)+(spec|test).[jt]s?(x)?(.snap)",
      "!{projectRoot}/tsconfig.spec.json",
      "!{projectRoot}/jest.config.{js,ts,cjs,cts,mjs,mts}",
      "!{projectRoot}/src/test-setup.[jt]s",
      "!{projectRoot}/test-setup.[jt]s"
    ],
    "sharedGlobals": [
      "{workspaceRoot}/tsconfig.base.json",
      "{workspaceRoot}/eslint.config.mjs",
      "{workspaceRoot}/.prettierrc"
    ]
  },
  "targetDefaults": {
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"]
    },
    "lint": {
      "cache": true,
      "options": {
        "max-warnings": 0
      },
      "// inputs": "`^default` is what makes the typed lint rules honest. `eslint.config.mjs` turns on `projectService: true` for `apps/garage/api/**` and `libs/**`, so `@typescript-eslint/no-floating-promises` reads types **across project references** — a signature change in a dependency changes a dependent's lint result. Without `^default` a project's lint hash covers only its own files, and that is not a theory: probed by making a `libs/garage/shared-types` export `async` while touching nothing in `apps/garage/api`, which then held a genuine floating promise and still reported `1/1 cache hit (100%)`, green. `--skip-nx-cache` on the same tree errored. A rule that reads as enforcement and enforces nothing is the exact failure `eslint.config.mjs` documents having been caught in four times. The price is more local cache misses; CI runs cold anyway.",
      "inputs": [
        "default",
        "^default",
        "{workspaceRoot}/eslint.config.mjs",
        "{projectRoot}/eslint.config.mjs",
        {
          "externalDependencies": ["eslint"]
        }
      ]
    },
    "test": {
      "cache": true,
      "inputs": [
        "default",
        "^production",
        {
          "externalDependencies": ["jest"]
        }
      ]
    },
    "typecheck": {
      "cache": true,
      "inputs": [
        "default",
        "^production",
        {
          "externalDependencies": ["typescript"]
        }
      ]
    },
    "e2e": {
      "cache": false
    }
  },
  "plugins": [
    {
      "plugin": "@nx/next/plugin",
      "options": {
        "startTargetName": "start",
        "buildTargetName": "build",
        "devTargetName": "dev",
        "serveStaticTargetName": "serve-static",
        "buildDepsTargetName": "build-deps",
        "watchDepsTargetName": "watch-deps"
      }
    },
    {
      "plugin": "@nx/eslint/plugin",
      "options": {
        "targetName": "lint"
      }
    },
    {
      "plugin": "@nx/playwright/plugin",
      "options": {
        "targetName": "e2e"
      }
    },
    {
      "plugin": "@nx/jest/plugin",
      "options": {
        "targetName": "test"
      },
      "exclude": ["apps/garage/api-e2e/**/*"]
    },
    {
      "plugin": "@nx/webpack/plugin",
      "options": {
        "buildTargetName": "build",
        "serveTargetName": "serve",
        "previewTargetName": "preview",
        "buildDepsTargetName": "build-deps",
        "watchDepsTargetName": "watch-deps",
        "serveStaticTargetName": "serve-static"
      }
    }
  ],
  "generators": {
    "@nx/next": {
      "application": {
        "style": "css",
        "linter": "eslint"
      }
    },
    "@nx/js": {
      "library": {
        "linter": "eslint",
        "unitTestRunner": "jest",
        "useProjectJson": true
      }
    },
    "@nx/react": {
      "library": {
        "style": "css",
        "linter": "eslint",
        "unitTestRunner": "jest",
        "useProjectJson": true
      }
    },
    "@nx/nest": {
      "library": {
        "linter": "eslint",
        "unitTestRunner": "jest",
        "useProjectJson": true
      }
    }
  }
}
